
struct Solution;

impl Solution {
    pub fn smaller_numbers_than_current(mut nums: Vec<i32>) -> Vec<i32> {
        nums.sort_unstable();
        let mut results : Vec<i32> = Vec::new(); 
        results
    }
}
